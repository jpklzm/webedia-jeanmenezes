Teste webedia Front-End
=======

Install the dependencies:

```npm install```

Check if everything is ok:

```npm run check```

Run in the development mode:

```npm start```

Run the build:

- For dev environment: ```npm run build```
- For prod environment: ```npm run build:prod```
